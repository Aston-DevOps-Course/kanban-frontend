name: Frontend CI/CD

on:
  push:
    branches:
      - main

jobs:

  build-and-push:
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Login Docker Hub
        run: docker login -u ${{ secrets.DOCKER_USER }} -p ${{ secrets.DOCKER_PASS }}

      - name: Build image
        run: docker build -t ${{ secrets.DOCKER_USER }}/kanban-frontend:latest .

      - name: Push image
        run: docker push ${{ secrets.DOCKER_USER }}/kanban-frontend:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:

      - name: Deploy to VPS
        uses: appleboy/ssh-action@master

        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}

          script: |
            cd /opt/kanban/task7
            docker compose pull frontend1 frontend2
            docker compose up -d frontend1 frontend2 nginx
